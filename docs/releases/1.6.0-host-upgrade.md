# Upgrade to reorder 1.6.0 (host handoff)

For a Medusa application already running this plugin at 1.5.0. Written against
what 1.6.0 actually ships; the host's own patch inventory and regression list
live in its `docs/plugins/2026-09-21-plugin-issues-for-source-repos.md` (§3
vendor patches, §4 regression list) and are the authoritative per-file list.

Three passages were written after 1.6.0 was published and describe what 1.6.1
adds, not what the published 1.6.0 tarball holds: *What lands in
`node_modules`*, *Read failures on the six `/store/saas/*` endpoints*, and item
5 of *After upgrading* (with their rows in the regression list). A host that
installs only the published 1.6.0 still gets the whole `.medusa/server` build
output in `node_modules`, still sees the pre-`readTenantScoped` failure answers
on those six routes, and its reconciliation still leaves a stale `SCHEDULED`
cycle chargeable; all three arrive together in 1.6.1.

## Cross-repo order — do not reorder

1. **Money units.** medusa-paypal's major/minor migration is merged in code, but
   its data migration (`medusa-saas/scripts/money-minor-to-major.sql`) has been
   dry-run only. Code and data must ship in the **same** release as the backend
   image: code that no longer scales amounts against a database that still stores
   minor units means charging 100× too much. This plugin contains no ÷100/×100
   compensation and must not gain one.
2. Publish and install `@mengyyy369/medusa-paypal@0.5.0`.
3. Upgrade the host to it and remove the vendor patches for it (§3).
4. Publish and install `@mengyyy369/reorder@1.6.0`.

Installing 1.6.0 before paypal 0.5.0 works, but native mirror rows will not
receive revised-plan events until step 2 lands — the reconcile job reports plan
changes in the meantime, so this degrades visibly rather than silently.

## What lands in `node_modules`

The tarball ships `.medusa/server/src` with every `__tests__` directory left out
— the compiled plugin the host imports, and nothing else from the build output.
Earlier versions packed all of `.medusa/server`, so an install also wrote the
compiled Playwright specs, `playwright.config.js`, the compiled `scripts/` tree
and the 28 compiled `src/**/__tests__/*.spec.js` unit specs into
`node_modules/@mengyyy369/reorder`. Measured against one and the same build
output: 400 files / 929.7 kB before, 366 files / 826.7 kB with the `src`
narrowing, 338 files / 738.4 kB with `"!**/__tests__/**"` in `files` as well
(5.0 MB unpacked → 4.5 MB → 4.0 MB), and `tar -tzf` on that final tarball lists
no `__tests__` entry. The negation is written without a `./` prefix because the
prefix makes npm ignore the pattern — on one and the same tree,
`!./**/__tests__/**` packs 366 files and `!**/__tests__/**` packs 338. The
import surface did not move: the `exports` map is byte-identical to 1.5.0's, and
every target under it still lands inside `.medusa/server/src` — `./workflows`,
`./modules/*`, `./.medusa/server/src/modules/*`, `./admin`, the `./*` catch-all
(`@mengyyy369/reorder/jobs/manual-renewal-hygiene` resolves across a slash) and
`./package.json` were each resolved through Node's own resolver against the
338-file tree and each returned a file. No host configuration changes with this
upgrade.

What a host cannot import, and could not before either:
`@mengyyy369/reorder/providers/<name>`. The plugin declares no providers —
`src/providers/` holds only the Medusa template README — so that key's target
matches no packed file and `require.resolve` answers `MODULE_NOT_FOUND`. The
packaging check is `scripts/assert-package-surface.mjs` (a repo-side script, it
is not shipped in the tarball), run as `npm run verify:package` or
`corepack yarn verify:package`: it packs, extracts the tarball with the system
`tar` — retrying with GNU tar's `--force-local`, the only form under which that
tar opens a drive-letter archive path — and exits non-zero on any `exports`
target missing from the packed tree, on any target left under `.medusa/server`
outside `src` (the path class `files` no longer ships), on any pattern target
matching no packed file unless the script lists it by name in its
`EMPTY_PATTERN_ALLOWLIST` — that is where the `./providers/*` target sits, and it
is printed as exempted rather than skipped quietly — and on a packed manifest
whose `exports` leave the check with nothing to compare.

## Database

Two migrations ship in this release. Both apply through the host's normal
`yarn medusa db:migrate` run (`README.md:147-159`), and the second one removes
rows, so read both before upgrading a database that has live renewals.

### 1. Activity log — `src/modules/activity-log/migrations/Migration20260922120000.ts`

`up()` drops `subscription_log_event_type_check` and re-adds it with 26 event
types, the added value being `subscription.creation_failed`
(`Migration20260922120000.ts:10-15`; the list matches the 26-member
`ActivityLogEventType`, `src/modules/activity-log/types/index.ts:8-35`), then
drops NOT NULL from `subscription_log.subscription_id` and
`.subscription_reference` (`:19-24`), which is what lets a creation failure be
recorded before any subscription row exists (both columns are already nullable on
the model, `src/modules/activity-log/models/subscription-log.ts:12,17`).

`down()` is destructive by design, and its statement order is the load-bearing
part (`Migration20260922120000.ts:27-46`): drop the constraint (`:28-30`), **then
delete every `subscription.creation_failed` row** (`:35-37`), then re-add the
constraint with the 25-value pre-1.6.0 list (`:38-40`), then restore NOT NULL on
the two display columns (`:41-46`). The delete has to precede the
`ADD CONSTRAINT`, because `ADD CONSTRAINT … CHECK` validates the rows already in
the table: run the other way round it aborts the rollback on any database holding
one of those rows. That is why this file was corrected in place rather than
superseded: `up()` is unchanged, so a host that already ran it has nothing to
re-apply. Rolling back still means losing those rows.

### 2. Renewal — `src/modules/renewal/migrations/Migration20260924120000.ts`

`up()` normalizes and only then constrains
(`Migration20260924120000.ts:21-87`):

- **It soft-deletes duplicate upcoming cycles.** For each subscription, every
  live (`deleted_at is null`) `renewal_cycle` row in status `scheduled` beyond the
  first one is stamped `deleted_at = now()`, `updated_at = now()` and
  `last_error = 'normalized: duplicate upcoming cycle removed by the 1.6.0
  uniqueness migration'` (`:55-82`, the marker text at `:78`). The row kept is the
  one whose `scheduled_for` already equals `subscription.next_renewal_at`,
  falling back to the most recent one and, among rows sharing that date, to the
  lowest cycle id: the ranking is entitlement match (`prefer_entitlement`,
  `:62`), then `rc."scheduled_for" desc`, then `rc."id"` ascending
  (`:65-74`, the ordering keys at `:69`). Duplicates are soft-deleted
  rather than flipped to `failed`, because the scheduler's due set selects
  `status in [scheduled, failed]`
  (`src/modules/renewal/utils/scheduler-query.ts:64`) and a `failed` row would go
  straight back into the charge queue. What the migration removed stays
  queryable afterwards through that `last_error` marker.
- **Then the invariant becomes a constraint.** It creates the partial unique index
  `renewal_cycle_one_scheduled_per_subscription` on `renewal_cycle (subscription_id)`
  restricted to `status = 'scheduled' and deleted_at is null` (`:84-86`) — one
  upcoming renewal cycle per subscription, enforced at write time. Normalizing
  first is not cosmetic: creating that index on a drifted database fails outright,
  which would break the upgrade of any host already carrying a duplicate. The
  behavior it pins down is documented in `docs/architecture/renewals.md`
  (*The one-upcoming-cycle invariant*).

`down()` drops the index and nothing else (`:96-100`). The normalized rows stay
soft-deleted and are **not** resurrected: a second chargeable cycle back on the
scheduler's list is worse than the asymmetry, so a rollback removes the
constraint without undoing the repair.

Two host-side cautions about this file:

- The index is hand-authored on purpose. The model generator cannot express the
  extra `status` predicate, so a later `medusa plugin:db:generate` has no model
  to compare it against and may propose dropping it — that drop is a regression,
  not cleanup (`Migration20260924120000.ts:11-19`).
- Plugin module migrators run in alphabetical module order, so this migration can
  run on a fresh database before the `subscription` table exists. Its
  entitlement-date lookup is therefore guarded by `to_regclass('"subscription"')`
  and degrades to newest-first ordering when the table is absent (`:35-40,60-63`);
  the quoted bare name — not `to_regclass('public.subscription')` — is what makes
  the guard resolve through the host's `search_path` exactly like the statement
  beside it does (`:41-53`).

No migration changes the shape of `subscription` or `plan_offer`: the new offer
rules live in the existing `rules` jsonb column, and native mirror rows use the
existing `reference` column (`model.text().unique()`,
`src/modules/subscription/models/subscription.ts:10`). The renewal migration
reads `subscription.next_renewal_at` (`Migration20260924120000.ts:60-62`) and
writes no `subscription` column.

## HTTP status changes on the three workflow-backed Store endpoints

`/store/saas/renew`, `/store/saas/auto-renew` and `/store/saas/redeem` changed
**which status they answer with** for a failure that is not one of this plugin's
own customer refusals. This is a contract change at release level, not an
internal cleanup: a bridge screen that today branches on `400` to mean "that
renewal is already in flight" or "there is no such subscription" stops matching
on this version, because both now answer with the status the error was thrown
as. Check that code before consuming 1.6.0.

What did **not** change: a declared refusal still answers **400 with its own
text**. That is the status these endpoints have always used for a refusal the
SaaS shows to the end customer, and the wording is pinned because the refusal is
declared verbatim. Each workflow owns its own list; no route decides on its own
what may be quoted:

| endpoint | declared refusals — 400, own wording |
| --- | --- |
| `POST /store/saas/renew` | the three `RENEW_CUSTOMER_REFUSALS` entries: mirror row, not in `manual` mode, not `active` (`src/workflows/create-manual-renewal.ts:46-62`) |
| `POST /store/saas/auto-renew` | the two `AUTO_RENEW_CUSTOMER_REFUSALS` entries: mirror row, overdue row (`src/workflows/set-subscription-auto-renew.ts:51-62`) |
| `POST /store/saas/redeem` | the nine `REDEEM_CUSTOMER_REFUSALS` entries, all raised by `resolve-redemption-code` (`src/workflows/redeem-redemption-code.ts:61-107`) |

Everything outside those lists keeps the HTTP semantics of the `MedusaError` type
it was thrown as — `not_found` 404, `invalid_data` and `not_allowed` 400,
`conflict` 409, `duplicate_error` and `payment_authorization_error` 422 — and
only what is none of those (driver and connection faults, deserialized Postgres
errors, `database_error`, `unauthorized`/`forbidden`, `unexpected_state`,
`invalid_argument`, an unrecognized shape) collapses to a **500**. In every
non-refusal case the body carries one of the route's own fixed strings and never
the engine's message; the step name plus the serialized error go to the server
log. The rule is implemented once, in
`src/workflows/utils/store-step-failure.ts` (`classifyStepFailure`, the
`PRESERVED_TYPES` table it reads, and `logUnquotedStepFailure`), and the
per-endpoint status inventory is documented in `docs/api/saas-bridge.md`
(*Failure disclosure on the three workflow-backed endpoints*).

The specific moves a host will observe, each cited to the throw site and to the
http spec that pins it where one exists:

- `POST /store/saas/renew` — `Renewal '…' is already processing` is thrown as a
  `conflict` (`src/modules/renewal/utils/errors.ts:23-28`, raised at
  `src/workflows/steps/create-manual-renewal.ts:188`) and answers **409**, where
  1.5.0 collapsed every workflow failure into a 400 that echoed the step's
  message (`v1.5.0` `src/api/store/saas/renew/route.ts:95-98`). It is
  retryable — that is what the status now says (test at
  `integration-tests/http/saas-bridge.spec.ts:744`, status asserted at `:770`).
  A 409's body text is core's own retry sentence rather than this plugin's, so
  branch on the status and not on the wording.
- `POST /store/saas/renew` — a subscription that vanishes between the handler's
  existence check and the step's own read answers **404** with the route's
  `subscription not found` (`src/workflows/steps/create-manual-renewal.ts:118-120`;
  test at `integration-tests/http/saas-bridge.spec.ts:865`, status asserted at
  `:903`), where 1.5.0 answered a 400 quoting the subscription id.
- `POST /store/saas/redeem` — an undeclared `not_found`, such as a batch whose
  variant row is gone (`src/modules/redemption/utils/errors.ts:12-13`, raised at
  `src/workflows/steps/redeem-redemption-code.ts:140`), answers **404** with the
  route's `redemption target not found`
  (`src/api/store/saas/redeem/route.ts:38-42`), while a code the store does not
  know stays **400** because that refusal is declared
  (`src/workflows/redeem-redemption-code.ts:63-66`; test at
  `integration-tests/http/saas-bridge.spec.ts:1401`, statuses asserted at
  `:1450-1451`). The customer session route runs the same workflow and classifies
  it the same way (`src/api/store/customers/me/redemptions/route.ts:62-79`), but it
  passes `preserveQuotedStatus`, so a declared refusal keeps the type the step threw
  it as and the same invalid code stays a **404** there — what that route changed is
  only the wording of a failure it does not author.
- `POST /store/saas/auto-renew` — 1.5.0 rewrote the mode from the request
  handler, so its only answers were its own inline 400s plus the body and
  tenant-ownership checks. The toggle now runs the `set-subscription-auto-renew`
  workflow, whose write step re-reads the row before updating
  (`src/workflows/steps/set-subscription-auto-renew.ts:177-183`): a row that
  vanished in between is a **404** and a `conflict` a **409**, each carrying the
  route's fixed text (`src/api/store/saas/auto-renew/route.ts:42-46`), and a
  driver fault is a **500**. The two guard refusals keep 400 but their wording
  now names the subscription id
  (`src/workflows/set-subscription-auto-renew.ts:55,60`, pinned at
  `integration-tests/http/native-subscription-mirror.spec.ts:410`), which matters
  only to a host matching the body instead of the status.

The statuses a request never gets to a workflow for are untouched: the body
validators still answer 400 (`src/api/store/saas/auto-renew/route.ts:81-92`,
`src/api/store/saas/renew/route.ts:85-90`,
`src/api/store/saas/redeem/route.ts:84-95`) and a foreign tenant is still a 404
that does not leak existence (`assertTenantVisible`,
`src/api/store/saas/lib/tenant-ownership.ts`).

## Read failures on the six `/store/saas/*` endpoints

A second boundary, separate from the workflow classification above, covers the reads
these six routes make themselves — the tenant check's `listSubscriptions` /
`retrieveCustomer` / `listCustomers`, `carts`'s and `reconcile`'s `query.graph` reads,
and the shared helper's customer read. Each is now made through `readTenantScoped`,
which asks `classifyStoreReadFailure`
(`src/modules/subscription/utils/store-read-failure.ts`) what a failed read may say,
and that function consults nothing about the error: the answer is `not_found` with
the route's own fixed sentence, while the raw cause goes to the server log.

**Read this as the host, before you consume 1.6.0: a database outage on these
tenant-scoping reads now answers 404.** It is not an accident of the rewrite and no
500 will surface it — masking a fault as an absence is the accepted decision, risk
**R1** in §D of
`.agents/specs/2026-09-25-post-acceptance-backlog.md`, taken because a fault that
answers differently from an absence is a probeable difference and the alternative
was a 400/422 quoting a table and column to the SaaS. The log line
`[reorder] <context>: tenant-scoped read failed` is where the cause is, and it is the
only place it appears.

| Endpoint | Read fault answers | What it answered before |
| --- | --- | --- |
| `POST /store/saas/auto-renew` | **404** `subscription not found` | 500, or 400/422 naming a table and column |
| `POST /store/saas/renew` | **404** `subscription not found` | 500, or 400/422 naming a table and column |
| `POST /store/saas/redeem` | **404** `redemption target not found` | 500 / internals-quoting 400; a gone customer answered **404** with core's `Customer with id '…' was not found`, and the id stayed in the text |
| `POST /store/saas/carts` | **404** `customer not found for this tenant`; **404** `No region configured for currency '<code>'` on a region *fault* | 500 / internals-quoting 400. Genuine absence of a region keeps its **400** with that same sentence — only the fault moved to 404 |
| `POST /store/saas/ensure-customer` | **404** `customer not found for this tenant` — a status this route did not answer before, reachable only on a faulted candidate read | 500 / internals-quoting 400. "No candidate found" is unchanged and still ends in an adopted or created customer |
| `POST /store/saas/reconcile` | **404** with that branch's own sentence: `Order '<id>' not found` / `Subscription '<id>' not found` / `Customer '<id>' not found` on the id lookups, `<resource> not found for this tenant` on the tenant check and the response reads | 500 / internals-quoting 400 on any of the route's nine reads |

What the boundary does **not** reach, and where a host sees no change at all: every
admin route, the store routes under `src/api/store/customers/me/**` (their customer
id comes from `req.auth_context.actor_id`, so a masked 404 would deny a caller a
resource it owns — the reads there are classified and left on core's error path on
purpose), and any store read that answers no tenant-scoping question. A declared
workflow refusal keeps its 400 on the bridge and its own status on the
customer-scoped route, exactly as in the previous section.

## After upgrading

1. **Remove the host's vendor patches** for the three areas this release absorbs
   — the tenant-ownership routes, the store subscription list serializer, and the
   `order.placed` subscriber — except the lines §3 marks as still mirrored in
   TypeScript. Leaving a patch in place shadows the plugin fix.
2. **Seed native mirror rows**, or let the hourly `native-subscription-backfill`
   job do it. It no-ops when medusa-paypal is absent. Until it has run, the two
   checkout guards cannot see provider subscriptions that predate the plugin.
3. **Confirm a single React** in the admin bundle: `pnpm why react` in the backend
   must resolve exactly one 18.x. Two copies is a white-screen class of failure,
   not a warning.
4. **Configure the three new offer rules only where wanted.** Every default is the
   pre-1.6.0 behavior: consent never flips automatically, a repeat purchase extends
   the existing row, and there is no stacking ceiling.
5. **Expect a retirement line in the log.** Upgrading puts the plugin in a position to
   retire a stale upcoming cycle it used to leave chargeable: when the reconciliation
   step keeps or moves one `renewal_cycle` row and finds a live `scheduled` row beside
   it, it soft-deletes that one and writes
   `[reorder] retired N stale upcoming renewal cycle(s) of subscription '<id>' (<cycle ids>) behind '<kept or moved cycle id>'`.
   The row keeps its id, its `renewal_attempt` children, its `status` and its
   `scheduled_for`; only `deleted_at` is stamped, so it leaves the Admin renewals queue
   and `GET /admin/renewals/:id` answers `404 not_found` for its id, while it stays
   readable through SQL. Two further lines complete the story: `withheld …` names a
   row the run selected but did not delete, because a renewal order landed on it
   between the two reads, and `restored N retired upcoming renewal cycle(s) …` means a
   workflow failure rolled the retirement back and those rows became chargeable again. **A retirement line is not
   evidence that this release's index is missing** — a `succeeded` or `failed` cycle on
   the subscription's `next_renewal_at` with one live `SCHEDULED` neighbour satisfies
   no part of the partial unique predicate and retires on a healthy database. A
   subscription holding **two live `SCHEDULED` cycles may indicate** the index was
   dropped, since that pair is exactly what
   `renewal_cycle_one_scheduled_per_subscription` refuses; check with
   `select subscription_id, count(*) from renewal_cycle where status = 'scheduled' and
   deleted_at is null group by subscription_id having count(*) > 1`, which returns
   nothing while the constraint stands.

## Regression list additions for this release

Beyond the host's §4 list, these are the behaviors 1.6.0 changes:

| Check | Expected |
| --- | --- |
| Checkout gate, both states | with a `NATIVE-` row `active`/`paused` for the product: the one-time purchase is refused `400` and **no order is created**; with `past_due`/`cancelled`: it completes normally |
| Repeat purchase | second purchase of the same product leaves **one** active row with `next_renewal_at` pushed out and `metadata.cycles_purchased` incremented, and **one** live `SCHEDULED` renewal cycle for it, with `scheduled_for` equal to the new `next_renewal_at` — the cycle that already existed is moved, not replaced. A second live `SCHEDULED` row is the double charge this release's index makes unwritable; a different product still gets its own row |
| Stale cycle retirement | a live `SCHEDULED` cycle the reconciliation leaves behind is soft-deleted with a `[reorder] retired …` log line naming both the row that went and the row it made room for, drops out of `GET /admin/renewals`, and keeps its `renewal_attempt` rows; the `having count(*) > 1` query above stays empty |
| Consent flip | with `consent_from_session` on and the session carrying the field, payment mode and mechanism change in the same update as the stored method; the activity log names the field |
| Forced renewal on a mirror row | admin force-renewal of a cycle belonging to a `NATIVE-` row is refused, not charged |
| Write side | `POST /store/saas/auto-renew` and the payment-method update both refuse a `NATIVE-` row, and the row's `payment_mode` is unchanged afterwards |
| Store list fields | `GET /store/customers/me/subscriptions` returns `frequency_interval`, `frequency_value`, `payment_mode`, `has_payment_method` |
| Creation failure trail | an order with no subscription row has a matching `subscription.creation_failed` log line naming the step, and re-running the subscriber does not duplicate it |
| Money, three-way | PayPal charged amount = admin order total = storefront displayed amount |
| Digital-goods checkout | a cart whose only address data is the region-seeded country stub completes, with a placeholder snapshot on the row |
| Reconcile foreign customer | `POST /store/saas/reconcile` with another tenant's `customer_id` answers `404`, not `{ subscriptions: [] }` |
| Failure status, `POST /store/saas/renew` | an already-processing renewal answers **409** and its body carries no cycle id; a subscription that vanished after this handler's existence check answers **404** with `subscription not found`; the three declared refusals still answer **400** in their own words |
| Failure status, `POST /store/saas/redeem` | an unknown code still answers **400** in its own words; a batch whose variant row is gone answers **404** with `redemption target not found`; no body ever carries the engine's message or a driver's `code`/`table`/`detail` |
| Failure status, `POST /store/saas/auto-renew` | the mirror and overdue guards answer **400** naming the subscription id; any other failure of the write step carries the route's own fixed text at the preserved status (**404**/**409**) or a **500** |
| Read fault, any of the six `/store/saas/*` endpoints | the route's own fixed sentence at **404**, and no body anywhere on those routes carrying a table name, a column name or core's `Customer with id '…' was not found`; the cause is in the server log line `[reorder] … tenant-scoped read failed`. A region that is genuinely unconfigured is still **400** |

## Production rehearsal

Measured on `170.106.132.210` on 2026-09-26, read-only, as `ubuntu` over key-based
SSH. Nothing below was executed against production in this pass; the mutating steps
are the procedure a later, separately gated session runs. `sudo` is not needed for
any of them: every `docker exec`, `docker inspect` and `docker compose` call in this
section answered directly as `ubuntu`, so adding `sudo` to a read-only probe only
makes it a privileged call.

### Where the image is built, and what that means for the credential

Both documented build methods are real, and **which one produced the running image
matters**:

- `apps/backend/Dockerfile.prod` and `apps/backend/Dockerfile` exist at
  `/home/ubuntu/medusa-saas/src/apps/backend/` (maxdepth 4; an earlier survey used
  maxdepth 2 and so "found no Dockerfile" — it is there). The build context is the
  **repo root of that pnpm workspace** (`/home/ubuntu/medusa-saas/src`, which holds
  `pnpm-workspace.yaml`, `pnpm-lock.yaml`, `apps/`), and the file says so at `:3-8`.
- `pnpm install` runs **inside the image build**, in the `deps` stage:
  `RUN pnpm install --frozen-lockfile --filter @medusa-saas/backend...`
  (`Dockerfile.prod:39`), against a `/root/.npmrc` assembled from a BuildKit
  `--secret` (`:25-34`). It does not run at container start and it does not run in
  the runtime stage — that stage only `COPY --from=build`s `/app/node_modules`
  (`:63`). So the registry credential is a **build-time** secret and belongs to
  whichever machine runs `docker build`.
- The running production image is **`medusa-saas-backend:0.4.20`**, not `0.4.14`
  (`compose.yaml:27`, `docker ps`, `StartedAt=2026-09-26T04:28:34Z`). Its contents
  are not buildable from anything on this box: it carries
  `@mengyyy369/medusa-better-auth@0.3.0` and a 219-line `medusa-config.ts` with no
  Logto entry, while the host workspace at the same moment pinned `0.2.4` and a
  239-line config that *does* register `@mengyyy369/medusa-logto`, and the host
  build cache holds **zero** records created on 2026-09-26 (all 58 records are dated
  2026-09-25). A grep of the verbose cache for `medusa-better-auth@0.3.0` answers 0,
  but so does one for `0.2.4`, so the date spread — not the version grep — is the
  load-bearing half of that evidence. Tags `0.4.16`–`0.4.20`
  are present on the box and on the local Windows workspace `D:\Projects\medusa-saas`
  with byte-identical image IDs and matching creation timestamps.
- So: **`0.4.20` was built off this host — on the local developer machine, by
  "方式 A" of the host's own `docs/deploy.md:26-28`** (`docker build` locally,
  `docker save | gzip`, `scp`, `docker load`). `docs/deploy.md:30-34` also documents
  a "方式 B" server-side build and it has genuinely been used for earlier tags
  (the Sep 25 cache records, plus `/tmp/medusa-saas-022.tar.gz`,
  `/tmp/medusa-saas-023.tar.gz`, `/tmp/medusa-saas-024.tar.gz` source archives from
  `git archive`). The build environment for the current image is therefore the
  developer machine, and its `~/.npmrc` is the `write:packages`-capable one. **It
  must not be copied onto the host to make a build "easier".** The host already
  keeps its own, separate credential: `/home/ubuntu/.medusa-npmrc`, mode 600, two
  lines, one `_authToken` whose SHA-256 differs from the local token's. Its
  *scope* (`read:packages` or something wider) cannot be measured without spending
  the token, so it is recorded as **UNKNOWN**; confirming it is the user's call, not
  a step to run here.
- One consequence worth stating plainly: this plugin is **not installed from the
  registry here at all**. `@mengyyy369/reorder` is declared `"workspace:*"`
  (`apps/backend/package.json:27`), locked as `link:vendor/@mengyyy369/reorder`
  (`pnpm-lock.yaml:68-70`), and present as a plain copied tree at
  `apps/backend/vendor/@mengyyy369/reorder`. So shipping 1.6.x to this host means
  replacing that vendored tree (or, separately, deciding to move it to a registry
  dependency), and no npm token is involved in that swap. The token question belongs
  to the sibling packages that *are* registry installs (`medusa-better-auth`,
  `medusa-payment-epay`, `medusa-payment-gmpay`, `medusa-webhooks`).

### `medusa migrate` runs by hand

`docker inspect medusa-prod-store-1`: `Entrypoint=["docker-entrypoint.sh"]`,
`Cmd=["npx","medusa","start","-p","9000"]`, `WorkingDir=/app/apps/backend`, no
healthcheck (`Healthcheck=null`). `@medusajs/medusa/dist/commands/start.js` contains
no migration call at all — grepping it for `migrat`/`MIGRAT` returns nothing — and
`/opt/medusa-prod/compose.yaml` has no migrate command. `medusa db:migrate` exists in
the image (`npx medusa --help`) and the host's runbook invokes it as a one-off
container (`docs/deploy.md:64`). **Migrations therefore never run at start: a new
image booted against the old schema simply answers errors.** The install is not done
until step 4 has run.

The image's `db:migrate` also accepts `--all-or-nothing` (default `false`), whose
own `--help` says: "If set, the command will fail if any migration fails and revert
the migrations that were applied so far" — that is the only thing standing between
the failure branch below and a partially migrated schema, so step 4 passes it
explicitly.

### What is actually already in production — the first-install premise is false

The earlier survey concluded production holds no `@mengyyy369/*` at all, from
`ls /app/node_modules/@mengyyy369`. That path is real and absent — but it is not
where the packages live. The app root is `/app/apps/backend`:

```
$ docker exec medusa-prod-store-1 ls /app/apps/backend/node_modules/@mengyyy369
medusa-better-auth
medusa-payment-epay
medusa-payment-gmpay
medusa-paypal
medusa-webhooks
reorder
```

`/app/apps/backend/node_modules/@mengyyy369/reorder` is a symlink to
`../../vendor/@mengyyy369/reorder`, whose `package.json` reads
`"name": "@mengyyy369/reorder"`, `"version": "1.5.0"`. It is registered as a plugin —
in the running image's `/app/apps/backend/medusa-config.ts` the entry is
`resolve: "@mengyyy369/reorder"` at line 111 (host working tree: `plugins: [` at
`:52`, the same entry at `:113`; the two files differ, see the build-location
bullet). Its tables exist (`subscription`,
`subscription_log`, `renewal_cycle`, `renewal_attempt`, `subscription_metrics_daily`,
`subscription_settings`, the link tables), and **20 of this plugin's 22 migrations
are already recorded** in `mikro_orm_migrations` (table columns: `id`, `name`,
`executed_at`; 208 rows total across all modules). Diffed by name, exactly two are
missing: `Migration20260922120000` (activity-log) and `Migration20260924120000`
(renewal) — which is precisely what 1.5.0's shipped set lacks, since
`find .medusa -name 'Migration*.js' | sort -u` over the vendored tree yields 20 files
ending at `Migration20260909140000`.

So a 1.6.x install here is an **upgrade from 1.5.0 with real renewal data**, not a
first install. Correcting the plan matters in both directions: the normalize/drift/
index-dedup paths are *not* unreachable, and `mikro_orm_migrations` is *not* expected
to gain 22 rows — it is expected to gain exactly two, named above. Baseline, measured
read-only: 16 live `subscription` rows, `renewal_cycle` by status `scheduled` 13 /
`succeeded` 5, **0** rows with `deleted_at is not null`, and 0 rows where more than
one live `scheduled` cycle shares a subscription.

Which assertions that leaves vacuous, and which it does not:

- **Vacuous on the current data unless rows are seeded deliberately**: the
  normalization that soft-deletes duplicate upcoming cycles. `having count(*) > 1`
  returns nothing today, so `up()` will normalize zero rows and the index will be
  created over a set that already satisfies it. A successful migrate on this database
  proves the *constraint lands*, not that the dedup picks the right survivor —
  that is §B harness coverage (`integration-tests/http/migrations.spec.ts`), and
  nothing done on this host substitutes for it. To exercise it here you would
  INSERT a second live `scheduled` cycle for one subscription *before* migrating,
  and then it is a mutation of production data and belongs behind its own gate.
- **Not vacuous**: the `to_regclass('"subscription"')` guard takes its
  non-degraded branch, because `subscription` exists and 13 live scheduled cycles
  join to it; the entitlement-date preference ordering is therefore genuinely
  reachable.
- **Already satisfied, and worth pinning before the migrate rather than after**:
  `renewal_cycle_one_scheduled_per_subscription` does **not** exist today
  (`pg_indexes` lookup → `0 rows`), and `subscription_log` is on the pre-1.6.0
  25-value `subscription_log_event_type_check` with `subscription_id` and
  `subscription_reference` both `is_nullable = NO`, with 0
  `subscription.creation_failed` rows. That is the correct pre-state for both
  pending migrations; recording it is what makes the post-state check mean
  something.

### Runbook steps

Every `<...>` is a value the operator fills from the box itself; no step below
substitutes a literal credential, and `POSTGRES_USER=medusa` / `POSTGRES_DB=medusa_store`
are quoted because they were read structurally (`docker exec medusa-prod-db-1
printenv POSTGRES_USER` → `medusa`, `printenv POSTGRES_DB` → `medusa_store`), not
from `.env`. The DB password is never referenced: every psql call in this runbook
goes through `docker exec` into the already-authenticated `medusa-prod-db-1`, so
there is no place a password would be typed, and none ever needs to leave the box.

1. **Snapshot the rollback anchors.** `cd /opt/medusa-prod && cp .env
   .env.bak.$(date +%F-%H%M) && docker images --format '{{.Repository}}:{{.Tag}}' |
   grep medusa-saas-backend` and record the current tag (`0.4.20`). Proof: a new
   `.env.bak-*` file (`ls -la /opt/medusa-prod/.env.bak-*`) and the tag list
   naming `0.4.20` plus the `rollback-*` tags that already exist
   (`rollback-20260925`, `rollback-20260925b`, `rollback-20260926`). Rollback needs
   no new save: the old image is already on the box.
2. **Pin the pre-migrate schema state** (read-only, run it now, keep the output):
   ```bash
   docker exec medusa-prod-db-1 psql -U medusa -d medusa_store -c \
     "select indexname from pg_indexes where indexname = 'renewal_cycle_one_scheduled_per_subscription'"
   docker exec medusa-prod-db-1 psql -U medusa -d medusa_store -c \
     "select conname, pg_get_constraintdef(oid) from pg_constraint \
        where conrelid = 'subscription_log'::regclass and contype = 'c'"
   docker exec medusa-prod-db-1 psql -U medusa -d medusa_store -c \
     "select subscription_id, count(*) from renewal_cycle \
        where status = 'scheduled' and deleted_at is null \
        group by subscription_id having count(*) > 1"
   docker exec medusa-prod-db-1 psql -U medusa -d medusa_store -c \
     "select m.name, case when e.name is null then 'MISSING' else 'applied' end as state \
        from (select unnest(array['Migration20260327143452','Migration20260329104245', \
              'Migration20260329153000','Migration20260329185930','Migration20260329194835', \
              'Migration20260330190157','Migration20260401143914','Migration20260401204521', \
              'Migration20260402183116','Migration20260403121004','Migration20260403132519', \
              'Migration20260405112000','Migration20260405112500','Migration20260410191100', \
              'Migration20260524220933','Migration20260524225625','Migration20260818120000', \
              'Migration20260909120000','Migration20260909130000','Migration20260909140000', \
              'Migration20260922120000','Migration20260924120000']) as name) m \
        left join mikro_orm_migrations e on e.name = m.name order by m.name"
   ```
   Proof, each measured here on 2026-09-26: query 1 → `0 rows`; query 2 →
   `subscription_log_event_type_check` listing 25 event types and **no**
   `subscription.creation_failed`; query 3 → `0 rows`; query 4 → 22 rows, of which
   exactly 20 read `applied` and the two 1.6.x names read `MISSING`. Do **not**
   narrow this to `where name like 'Migration2026%'`: measured, 44 such names exist
   and core modules own some of them (`Migration20260106185528`,
   `Migration20260224120000`), so a `like`-filtered count is not the plugin's
   state. Note the table is
   `renewal_cycle`; the name `subscription_renewal_cycle` does not exist and psql
   answers `ERROR: relation "subscription_renewal_cycle" does not exist` (measured),
   which is why step 6's queries name the bare table.
3. **Build the new image where it is built.** On the machine whose workspace produced
   `0.4.20` (measured: the local `D:\Projects\medusa-saas`, at a commit whose
   `medusa-config.ts` is the one the running image carries), replace
   `apps/backend/vendor/@mengyyy369/reorder` with the 1.6.x build and run the
   documented 方式 A sequence (`docs/deploy.md:21-28`): `docker build -f
   apps/backend/Dockerfile.prod --secret id=npmrc,src=$HOME/.npmrc -t
   medusa-saas-backend:0.4.21 .`, then `docker save … | gzip | scp | docker load`.
   Proof inside the image before it ships, both read-only: `docker run --rm
   --entrypoint sh medusa-saas-backend:0.4.21 -c 'grep -E "\"version\""
   /app/apps/backend/vendor/@mengyyy369/reorder/package.json'` → `1.6.x`, and
   `… ls /app/apps/backend/vendor/@mengyyy369/reorder/.medusa/server/src/modules/renewal/migrations/`
   → the listing now contains `Migration20260922120000.js` **and**
   `Migration20260924120000.js`, which 1.5.0's tree does not. If either check fails
   the vendor swap did not take and nothing further runs.
4. **Swap the tag and migrate, in that order.** Edit `/opt/medusa-prod/compose.yaml`
   `image:` to the new tag (the service is named **`store`**, not `backend`; the
   host's own `docs/deploy.md:64` command line says `backend` and does not run
   against this compose file as-is), then:
   ```bash
   docker compose -f /opt/medusa-prod/compose.yaml run --rm --no-deps store \
     npx medusa db:migrate --all-or-nothing --execute-safe-links
   ```
   Proof: `Migrations completed` on stdout, and afterwards step 2's query 4 shows
   `Migration20260922120000` and `Migration20260924120000` present — the expected
   delta on this database is **exactly two rows**, not a fresh 22.
   `--execute-safe-links` is there because `db:migrate` calls `syncLinks` after the
   migrations (`dist/commands/db/migrate.js:86-95`) and without it that step can
   prompt, which in a one-off container just hangs.
5. **Only then start the new image**: `docker compose -f /opt/medusa-prod/compose.yaml
   up -d store`. Proof: `docker inspect medusa-prod-store-1 --format
   '{{.Config.Image}} {{.State.StartedAt}}'` names the new tag, and `docker compose
   -f /opt/medusa-prod/compose.yaml logs --since 5m store` shows module loading with
   no `Workflow with id … already exists` and no schema-error stack.
6. **The two invariant queries, re-run unchanged from step 2.** (a) the `pg_indexes`
   lookup for `renewal_cycle_one_scheduled_per_subscription` must now return **1
   row**, and its `indexdef` must read `CREATE UNIQUE INDEX … ON public.renewal_cycle
   USING btree (subscription_id) WHERE ((status = 'scheduled') AND (deleted_at IS
   NULL))` — the predicate is the whole point, an index without it is the wrong
   index; (b) the `group by subscription_id having count(*) > 1` query must still
   return **0 rows** *and* the 13 live scheduled cycles from step 2 must still be
   13, not 12: `select count(*) from renewal_cycle where status = 'scheduled' and
   deleted_at is null`. A count that dropped while (b) stayed empty means a row was
   retired by the migration and it must be named
   (`where last_error like 'normalized:%'`) before anything else is believed.
7. **Money units — a stop point, not a step.** Production is still on **minor**
   units: `select amount, currency_code from price limit 10` returned `990|usd`,
   `69900|cny`, `5990|cny` (i.e. $9.90 / ¥699 / ¥59.90), and
   `raw_amount` carries the same numbers (`{"value": "990", …}`). The data migration
   `scripts/money-minor-to-major.sql` exists in the local `medusa-saas` workspace
   and **is absent on the host** (`find /home/ubuntu/medusa-saas -iname '*money*'`
   → nothing), and the vendored `@mengyyy369/medusa-paypal` is `0.4.0`, not the
   `0.5.0` the ordering above requires. So the plugin install in steps 3-6 must
   ship **without** any money-unit movement, and this batch — paypal `0.5.0` + the
   SQL + the host config — is decided separately with the user. A half-switched set
   is the shape of the earlier hundred-fold incident.
8. **Failure branch: the store came up on a partially migrated schema.** This is
   reachable by design and not hypothetical. Each migration is atomic
   (`up()` is transactional and all-or-nothing by MikroORM default,
   `@mikro-orm/core/utils/Configuration.js:89-91`, not overridden by Medusa at
   `mikro-orm-create-connection.js:89-92`), but the per-module migration loop is
   **not** one transaction: `runMigrations` is `for (const migration of
   runMigrationsFn.filter(Boolean)) { await migration.apply(...) }`
   (`@medusajs/modules-sdk/dist/loaders/utils/load-internal.js:309-317`, lines
   confirmed in the running image), so one module can finish and the next fail,
   leaving committed schema behind — and step 4 without `--all-or-nothing` does
   exactly that. Check, in order:
   1. `select name, executed_at from mikro_orm_migrations where name in
      ('Migration20260922120000','Migration20260924120000')` — which of the two
      landed (same two-name filter as step 2's query 4, for the same reason).
   2. The two step-6 queries, plus `select count(*) from renewal_cycle where
      deleted_at is not null` and `select conname, pg_get_constraintdef(oid) from
      pg_constraint where conrelid = 'subscription_log'::regclass and contype = 'c'`.
      If `Migration20260924120000` is *absent* while `deleted_at is not null`
      returns rows, the normalize committed and the index did not: the database is
      drifted-but-unconstrained, which is the state a re-run must not be allowed to
      "fix" by guesswork.
   3. If the activity-log migration landed and the renewal one did not, the safe
      recovery is `docker compose … run --rm --no-deps store npx medusa db:migrate
      --all-or-nothing` again — `up()` is idempotent on a schema already in the
      target shape and the constraint widening does not conflict with the renewal
      work. Do not hand-apply either migration's SQL.
   4. If rows were soft-deleted without the index landing, **stop**: do not re-run
      and do not restart the store to "see if it comes up". Roll the schema back to
      the step-2 snapshot from the dump Task 25 rehearses, or pin the removal
      explicitly (`select id, subscription_id, scheduled_for, last_error from
      renewal_cycle where deleted_at is not null`), because `down()` of
      `Migration20260924120000` drops the index and resurrects nothing.
   Proof you are out of the failure branch: all four of step 2's queries match their
   step-6 expected shapes, and `docker compose logs store` since the failing
   migration is re-run contains `Migrations completed`.

## Rehearsal results — executed on 2026-09-26, scratch database only

Everything below **ran** on `170.106.132.210` between 18:13 and 18:43 (host local
+0800 = 10:13–10:43 UTC), against a **restored copy** of the production database.
Production itself was not migrated, not restarted, and its vendored plugin stayed
at `1.5.0` throughout (verified after the last run: `docker exec medusa-prod-store-1
grep '"version"' /app/apps/backend/vendor/@mengyyy369/reorder/package.json` →
`1.5.0`; `medusa_store` still 208 `mikro_orm_migrations` rows, 0 soft-deleted
cycles, 0 copies of the unique index).

**The app was never booted.** No `medusa start`, `medusa develop`, or any command
that constructs the full Medusa app ran against the restored database, anywhere in
this rehearsal. The restored rows carry real customer payment references, and the
containers had the host's real payment-provider credentials in their environment;
a live scheduler pointed at that data can charge real customers. **A migrate-only
rehearsal proves the schema path; it is not end-to-end verification**, and no
result below should be read as covering subscriber, cron, or HTTP behavior.

### Dump and restore (steps 1–2)

```
docker exec medusa-prod-db-1 pg_dump -U medusa -d medusa_store -Fc \
  > /home/ubuntu/reorder-161-rehearsal-20260926/prod-20260926T1814Z-medusa_store.dump
# exit 0 in 0.54 s; chmod 600 immediately; mode -rw------- ubuntu:ubuntu
# size   655894 bytes (non-trivial: 1345 TOC entries; pg_dump 17.11 = server 17)
# sha256 8a5de66d8a59cf65b9911342dc5c2ef111118a27e7c7179fca8bfa20bd8b9957
```

The dump is the rollback artifact step 8 of the runbook depends on: it is
**retained** at that path, on the host only, never downloaded, contents never
printed. Deleting it is a user decision.

`CREATE DATABASE medusa_rehearsal_161 OWNER medusa` (on the `postgres` db, target
name printed before the command), then
`pg_restore -U medusa -d medusa_rehearsal_161 --no-owner --exit-on-error < dump`
→ exit 0, stderr 0 bytes (2.6 s). Post-restore counters equal the pre-migrate
production baseline exactly: `subs_live=16`, `cycles_total=18`, `sched_live=13`,
`mikro_rows=208`, index absent (`0`), `dupgroups=0`, `softdel=0`, plugin
migrations applied `20`, missing `Migration20260922120000` +
`Migration20260924120000`. The scratch database is **the same shape as
production**, so what follows rehearses the real upgrade, not a partial restore.

### Plugin delivery: scratch path + one-off container (step 3)

`pnpm add` is not this host's mechanism (the plugin is `link:vendor/…`), so the
1.6.1 source arrived as the **published-shaped tarball**: local `corepack yarn
build` (exit 0 at `cb88207`) + `npm run verify:package` (exit 0, 8 exports
targets) → `mengyyy369-reorder-1.6.1.tgz` (751307 bytes, sha256
`098ba9ad22a0a63fbc1cd180c1a07eb9138ff871e5ce71c9468d54e2268928bf`), scp'd to
`/home/ubuntu/reorder-161-rehearsal-20260926/` and extracted to `plugin/` there
(`"version": "1.6.1"`, 22 compiled `Migration*.js`, no `__tests__`). No image was
rebuilt or retagged, no `docker cp` into `medusa-prod-store-1`, no host file
edited. The throwaway container is `docker run --rm` of the unchanged
`medusa-saas-backend:0.4.20`, with the **scratch** `plugin/` directory bind-mounted
over `/app/apps/backend/vendor/@mengyyy369/reorder` and a `DATABASE_URL` derived
from the live container's own by replacing only the database component — printed
and asserted (`host=db db=medusa_rehearsal_161`) **before** any migrate command,
inside the wrapper script and again inside the container; the container aborts if
the parsed target is not the scratch name.

### The first real finding: `--all-or-nothing` reverts far more than it applies (attempt 1)

The first migrate run mounted the scratch plugin **read-only** (`:ro`) as the
fence's conservative reading required. Both 1.6.x migrations applied and passed,
then module `saas-bridge` failed — and the flag did exactly what its help text
says, at a scale worth recording:

```
npx medusa db:migrate --all-or-nothing --execute-safe-links   # → exit 1
{"level":"error","message":"Some migrations failed to run: Error: ENOENT: no such file or directory, mkdir '/app/apps/backend/vendor/@mengyyy369/reorder/.medusa/server/src/modules/saas-bridge/migrations'"}
```

Log: `migrate.log`, 51318 bytes, 18:27:50. The roll-back pass **reverted 42
already-applied migrations** (unique count; every module with a `down()`),
leaving `mikro_orm_migrations` at **169** — not the 208 baseline — because one
core migration (`sales_channel Migration20240115152146`) logs
`Skipped Migration20240115152146. Missing down method` and **stays applied**.
`--all-or-nothing` is therefore not a round trip on this stack: a failure mid-run
cannot be recovered by "just run it again"; it leaves a schema older than the one
the running app was built against, plus one stranded newer module. Step 8's
failure branch must budget for restoring the dump, not for a re-run. (The scratch
database was rebuilt from the retained dump after this: `DROP DATABASE … WITH
(FORCE)`, re-`CREATE`, re-`pg_restore` exit 0, counters re-asserted 16/18/13/208.)

Root cause of the failure itself: MikroORM's umzug calls
`mkdir(migrationsPath, {recursive:true})` inside `migrateUp()` for **every**
module — including `saas-bridge`, which has **zero migrations** and whose
`migrations/` directory therefore does not survive the 1.6.1 packaging narrowing
(empty directories are not packed). On a writable tree the mkdir succeeds and
creates an empty directory; under `:ro` it is the *recursive* form, so the
missing leaf reports `ENOENT` even though nothing would be written (reproduced
standalone: plain `mkdir` → `EROFS`, recursive → `ENOENT`). The host's production
vendor tree is a plain copied directory that **has** the empty
`saas-bridge/migrations/`, so this is an artifact of shipping the tarball instead
of that tree — not a schema problem.

### Migrate, assertions, idempotency (steps 4–6, attempts 2–4)

With the scratch plugin directory mounted **read-write** (the container is
throwaway; production paths stay untouched — verified afterwards by
`diff -rq` of a fresh tarball extraction against `plugin/`: the **only**
difference is the empty `saas-bridge/migrations/` directory umzug created):

```
bash /home/ubuntu/reorder-161-rehearsal-20260926/migrate2.sh > migrate4.log 2>&1
# exit 0; log 18:43:22
  MODULE: activityLog   ● Migrating Migration20260922120000  ✔ Migrated   Completed successfully
  MODULE: renewal       ● Migrating Migration20260924120000  ✔ Migrated   Completed successfully
  57 further modules:  "Skipped. Database is up-to-date for module."  (57 measured)
  "Migrations completed"        <- the exact success line, step 4's proof, as the runbook predicts
```

The delta is **exactly the two expected migrations and nothing else**: a full
`diff` of the ordered `mikro_orm_migrations` name lists taken before (208 rows)
and after (210 rows) shows precisely two added lines —
`Migration20260922120000`, `Migration20260924120000` — and no removed or changed
lines, so no unrelated module's migrations re-ran. All 22 plugin names are now
recorded (`plugin22_applied=22`). A third, immediate re-run against the migrated
scratch DB (18:38, `migrate3.log`) applied nothing ("up-to-date",
`Migrations completed`) — the second migrate is a no-op.

Post-migrate assertions on the scratch database (`post.sql`, exit 0):

| check | result |
| --- | --- |
| (a) `select indexname from pg_indexes where indexname='renewal_cycle_one_scheduled_per_subscription'` | **1 row**, `indexdef = CREATE UNIQUE INDEX … ON public.renewal_cycle USING btree (subscription_id) WHERE ((status = 'scheduled'::text) AND (deleted_at IS NULL))` — predicate present, the right index |
| (b) `group by subscription_id having count(*) > 1` (live `scheduled`) | **0 rows** |
| live `scheduled` count did not drop | **13** before and after; `succeeded` still 5; `cycles_total` still 18 |
| normalize retired | **0** rows gained `deleted_at` (`softdel_rows=0`), **0** rows carry the `last_error like 'normalized:%'` marker — the expected vacuous branch, verified not assumed |
| activity-log migration landed | `subscription_log_event_type_check` now lists 26 values ending `subscription.creation_failed`; `subscription_id`/`subscription_reference` both `is_nullable=YES`; 0 `creation_failed` rows |

**Step 6's non-vacuous claim:** the `CREATE UNIQUE INDEX` of
`Migration20260924120000`'s `DO $$`-guarded `up()` ran over a `renewal_cycle`
table holding **18 real production rows** (13 live `scheduled`, 5 `succeeded`,
`deleted_at` all null) whose `to_regclass('"subscription"')` join saw 16 live
subscriptions — the identical statement shape that failed acceptance in 1.6.0 on
a drifted table. It **succeeded on the existing data**, soft-deleted **nothing**,
and named rows: none (0 rows match `deleted_at is not null` post-migrate). The
entitlement-preference ordering branch was genuinely reachable (table present,
rows join), and it retired zero rows because production has zero duplicates.
What remains **unverified-in-rehearsal, by fence decision**: the dedup branch
picking the right survivor (needs a seeded duplicate ⇒ a production-data mutation
in the rehearsal copy's own pre-state; §B harness `integration-tests/http/
migrations.spec.ts` is the coverage), and everything that requires booting the
app (subscribers, cron, the `retired N stale upcoming cycle(s)` log line).

### Environment facts this rehearsal added

- The live store container's environment was captured key-wise (35 vars, values
  never printed) and the one-off ran `--user root` — which is also what
  `/opt/medusa-prod/compose.yaml` sets for `store` (`user: root`), so step 4's
  `docker compose run` inherits it. As `ubuntu` no `sudo` is installed and none
  was needed; no password prompt was ever encountered.
- The image has no psql and the scratch URL file is mode 600; all DB access stayed
  inside `medusa-prod-db-1` / bind mounts, and no credential left the box or
  appeared in any log, report, or tracked file.
- Telemetry banner prints on first `npx medusa` in a fresh container (harmless;
  opt-out is `medusa telemetry --disable`).
- `redisUrl not found. A fake redis instance will be used.` during `db:migrate` —
  expected, no locking involvement at migrate time.
- Scratch artifacts retained at `/home/ubuntu/reorder-161-rehearsal-20260926/`
  (dump, tarball, `plugin/`, `pre.sql`/`post.sql`/`livecheck.sql`,
  `preflight.sh`, `migrate2.sh`, logs `migrate.log`/`migrate2.log`/`migrate3.log`/
  `migrate4.log`, `migrations-before.txt`/`migrations-after-final.txt`,
  `store.env.base` mode 600, `rehearsal-dburl.env` mode 600). The scratch database
  `medusa_rehearsal_161` is **left in place, migrated** (210 rows), for Task 26.
  No throwaway container remains (`--rm`); the only non-`Up` container on the box
  (`medusa-prod-receiver`, Exited 137 four days ago) predates this rehearsal.

## Known gaps

- `paypal.subscription.revised` is not consumed yet (awaits paypal 0.5.0); the
  reconcile job is the interim detection for plan changes.
- A native row's `next_renewal_at` can be null when PayPal's `activated` path had
  no billing date. Nothing infers a date; Admin shows it as unresolved.
- The activity log reuses `subscription.created` for an extended row,
  distinguished by `metadata.trigger_type: "order_placed_extend"`; a dedicated
  `subscription.extended` event type would need its own CHECK-constraint migration
  and was left out of this release.
